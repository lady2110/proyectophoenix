<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.ProyectoFenix" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/naranjaclaro</item>
        <item name="colorPrimaryDark">@color/naranjaoscuro</item>
        <item name="colorAccent">@color/amarillolclaro</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="AppThemes.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>
    <style name="AppThemes.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />
    <style name="AppThemes.pOPUTOverlay"  parent="ThemeOverlay.AppCompat.Light" />

    <style name="AppThemes"></style>
</resources>